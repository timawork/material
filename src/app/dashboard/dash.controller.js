(function() {
    'use strict';

    angular.module('fm')
        .controller('dashCtrl',dashCtrl);

    dashCtrl.$inject = ['$scope']

    function dashCtrl($scope) {
        var vm = this;
    };

})();
